#!/bin/sh /etc/rc.common
# Copyright (C) 2006 OpenWrt.org

BIN=avahi-daemon
DEFAULT=/etc/default/$BIN
OPTIONS="-D"
RUN_D=/var/run/$BIN
[ -f $DEFAULT ] && . $DEFAULT

start() {
	mkdir -p $RUN_D
	$BIN $OPTIONS
}

stop() {
  $BIN -k
}

reload() {
  $BIN -r
}

